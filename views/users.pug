meta(charset="UTF-8")
meta(name="viewport", content="width=device-width,initial-scale=1")
title MWF - Users Table
link(rel="stylesheet", href="./css/users.css")
//  Navbar 
header
  h1 (MWF) - Users
  nav
    a(href="/dashboard") Dashboard
    a(href="/addstock") Add Stock 
    a(href="/stocklist") Stock List
    a(href="/addsales") Add sales 
    a(href="/saleslist") Sales List 
    //- a(href="getusers") Users 
    a(href="logout") Logout
main
  section.users-list
    h2 Registered Users
    table
      thead
        tr
          th Full Name 
          th Email 
          th Role
          th Date 
      tbody#users-table-body
        if users.length 
          each user in users 
            tr 
              td #{user.firstName+" "+user.lastName}
              td #{user.email}
              td #{user.role}
              td #{moment(user.date).format("DD-MM-YYYY")}

        //  Data will be dynamically populated from DB 
footer
  p © 2025 MWF Inventory System
script(src="/js/users.js")